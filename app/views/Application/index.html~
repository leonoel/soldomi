#{extends 'main.html' /}
#{set title:'Home' /}

#{if tunes.size()}
    
<div class="tunes">
  <h3>Tunes available :</h3>
  <ul>
  #{list items:tunes, as:'tune'}
  <li>${tune.title}</li>
  #{/list}
  </ul>
</div>
        
#{/if}
 
#{else}
    <div class="empty">
        No tune available.
    </div>
#{/else}

<form action="@{Application.uploadNwc()}" method="POST" enctype="multipart/form-data">
    <input type="file" id="nwc" name="nwc" />
    <input type="submit" value="Add file" />
</form>
